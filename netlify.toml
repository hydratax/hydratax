[build]
  command = "npm run build"
  publish = ".next"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Day-7 free-trial ending reminder emails (09:00 UTC daily)
[functions."trial-reminders"]
  schedule = "0 9 * * *"

[[plugins]]
  package = "@netlify/plugin-nextjs"
